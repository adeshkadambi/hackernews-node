type Query {
  info: String!
  feed: [Link!]!
  link(id: ID!): Link # find link by id
}

type Mutation {
  post(url: String!, description: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}